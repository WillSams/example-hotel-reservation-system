export NODE_ENV=development

export API_PORT=4000

export DB_CLIENT=postgresql

export DB_USER=postgres
export DB_PASSWD=postgres
export DB_HOST=localhost
export DB_PORT=15432
export DB_URL=postgres://${DB_USER}:${DB_PASSWD}@${DB_HOST}:${DB_PORT}/${DB_DEV}

export CACHE_HOST=127.0.0.1
export CACHE_PORT=6379
export CACHE_PASSWORD=p@ssw0rd123!

export MESSAGING_SECRET=s3cr3t
export MESSAGING_USER=guest
export MESSAGING_PASSWD=guest

export MESSAGING_QUEUE_PORT=5672
export MESSAGING_WEB_PORT=15672


